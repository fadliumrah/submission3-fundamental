<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fadli.submission3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Submission3"
        tools:targetApi="31">

        <activity
            android:name=".ui.favorite.Favorite"
            android:exported="false"
            android:label="Daftar Favorite"/>

        <activity
            android:name=".ui.themes.ChangeThemes"
            android:exported="false"
            android:label="Change Themes"/>

        <activity
            android:name=".ui.detailUser.DetailUser"
            android:exported="false" />

        <activity
            android:name=".ui.home.MainActivity"
            android:exported="false"
            android:label="GitHub User" />

        <activity
            android:name=".splash.SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.Submission3.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>